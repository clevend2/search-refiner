export const STORAGE_PREFIX = "search-refiner"

export const STORAGE_USER_ENABLED = `${STORAGE_PREFIX}-user-enabled`

export const STORAGE_DOMAINS = `${STORAGE_PREFIX}-domains`

export const IS_ENABLED_PORT = "is-enabled"

export const LOGGER_PORT = "logger"